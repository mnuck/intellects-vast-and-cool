#include <iostream>
#include "Spawn.h"
#include "AI.h"

using std::cout;
using std::endl;


void Spawn::activate()
{
    // find best spawn loc
    // bail if no available spawners (should never happen)
    // spawn a dood
}
